/**
 * User
 * @typedef {Object} User
 * @property {number} id User id
 * @property {string} name Username
 * @property {number | string} [age] The User's age (Optional)
 * @property {boolean} isActive User state
 */

/**
 * @type {User}
 */
const myNewUser = {
    id: 1,
    name: "Ryan",
    age: 30,
    isActive: true
}

/**
 * @type {User}
 */
const myNewUser2 = {
    id: 2,
    name: "Marcos",
    age: 32,
    isActive: false
}