/**
 * Esto es una lista de edades de usuarios
 * @type {Array}
 */
const age = [19, 20, 21, 22];

/**
 * Array of User's scores
 * @type {Array<Number>}
 */
const scores = [100, 90, 80, 60];

/**
 * Array of Numbers and Strings
 * @type {Array<Number | string>}
 */
const scoresAndStrings = [100, "100", 20, "30"];

/**
 * Array of names
 * @type {Array<string>}
 */
const names = ["rayn", "john", "joe"];

names.push("23");
